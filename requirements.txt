# --- Core data stack ---
pandas==2.2.2
numpy==1.26.4

# --- Extraction ---
duckdb==1.1.3            # SQL engine that reads the Open Food Facts Parquet directly
huggingface_hub==0.25.2 # to locate the OFF Parquet files on Hugging Face
pyarrow==17.0.0         # Parquet support
requests==2.32.3        # for the live API script

# --- Modeling (Nutri-Score classifier) ---
scikit-learn==1.5.2
xgboost==2.1.1

# --- Notebooks / charts (optional but handy) ---
jupyterlab==4.2.5
matplotlib==3.9.2
seaborn==0.13.2
